# AI News Intelligence Improvement Tasks

## Progress Tracker

- [x] 1. Fix requirements.txt (remove duplicates, fix `python-dotnev` typo)
- [x] 2. Fill in docker-compose.yml (orchestrate Redis, backend, worker, dashboard)
- [x] 3. Wire up news pipeline (news_fetcher.py fallback when no API key)
- [ ] 4. Add JWT authentication (login endpoint, protected routes)
- [ ] 5. Remove leftover artifacts (empty folder, duplicate news.db)
